<h1>Wines</h1>

<ol>
<% @wines.each do |wine| %>
  <li>
  <p><%= link_to(wine.name.presence || 'No Name', wine) %> (<%= wine.varietal %>) from <%= wine.winery %></p>
</li>
<% end %>
</ol>
<%= link_to 'Add a New Wine', new_wine_path %>